# Ruby-on-rails-Docker-and-docker-compose-set-up
This contains code in infrastructure as code to setup ruby on any system. It will then be app loaded to the docker hub as an image using github actions

